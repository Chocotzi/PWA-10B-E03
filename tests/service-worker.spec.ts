import { describe, it } from "vitest";

describe("service worker (public/sw.js)", () => {
  it.todo("S3.4: install precachea PRECACHE_URLS en CACHE_VERSION");
  it.todo("S3.4: activate elimina cachés con otro nombre");
  it.todo("S3.4: navegación sin red y sin copia responde /offline");
});
