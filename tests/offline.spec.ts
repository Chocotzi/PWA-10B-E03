import { describe, it } from "vitest";

describe("consulta offline y registro seguro", () => {
  it.todo("S3.5: la lista de inspecciones se consulta sin red desde la copia en caché");
  it.todo("S3.5: registerServiceWorker devuelve null fuera de producción y nunca lanza");
  it.todo("S3.5: activateWaitingServiceWorker envía SKIP_WAITING al worker en espera");
});
